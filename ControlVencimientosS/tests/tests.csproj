<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
    <RootNamespace>ControlVencimientos.Tests</RootNamespace>
    <AssemblyName>ControlVencimientos.Tests</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" Version="6.0.4" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.4" />
  </ItemGroup>

  <!-- ESTA es la única referencia que realmente faltaba en la solución.
       El proyecto de tests necesita ver al proyecto web para poder probar
       el cálculo de estado y la selección de hitos de aviso. -->
  <ItemGroup>
    <ProjectReference Include="..\ControlVencimientosP\ControlVencimientosP.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

</Project>
