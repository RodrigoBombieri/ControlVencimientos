@{
    ViewData["Title"] = "Sin acceso";
}

<div class="text-center mt-5">
    <h1 class="h3">No tenés acceso a esta página</h1>
    <p class="text-muted">Si creés que es un error, contactá al administrador.</p>
    <a asp-controller="Cuenta" asp-action="Login" class="btn btn-primary mt-3">Volver a ingresar</a>
</div>